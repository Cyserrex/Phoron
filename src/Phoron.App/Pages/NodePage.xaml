<UserControl x:Class="Phoron.App.Pages.NodePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid Margin="4,8,4,4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="160"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Node / TypeScript" Style="{StaticResource Judul}"/>
            <TextBlock x:Name="TxtInfo" Style="{StaticResource SubJudul}"
                       Text="Jalankan Next.js, Astro, Vite, dan proyek Node lain dari folder mana pun."/>
        </StackPanel>

        <ListView x:Name="Daftar" Grid.Row="1" Margin="0,0,0,10"
                  SelectionChanged="Daftar_Changed" MouseDoubleClick="Daftar_DoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Proyek" Width="150" DisplayMemberBinding="{Binding Nama}"/>
                    <GridViewColumn Header="Folder" Width="230" DisplayMemberBinding="{Binding Folder}"/>
                    <GridViewColumn Header="Kerangka" Width="85" DisplayMemberBinding="{Binding Kerangka}"/>
                    <GridViewColumn Header="Perintah" Width="120" DisplayMemberBinding="{Binding Perintah}"/>
                    <GridViewColumn Header="Status" Width="80" DisplayMemberBinding="{Binding Status}"/>
                    <GridViewColumn Header="Alamat" Width="170" DisplayMemberBinding="{Binding Alamat}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Skrip" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox Grid.Column="1" x:Name="CmbSkrip" MinWidth="130" Margin="0,0,16,0"
                          SelectionChanged="CmbSkrip_Changed"/>
                <TextBlock Grid.Column="2" Text="Node" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox Grid.Column="3" x:Name="CmbNode" MinWidth="200"
                          SelectionChanged="CmbNode_Changed"/>
            </Grid>
            <WrapPanel>
                <ui:Button x:Name="BtnJalan" Content="Jalankan" Appearance="Primary"
                           Margin="0,0,8,0" Click="BtnJalan_Click"/>
                <ui:Button Content="Tambah proyek..." Margin="0,0,8,0" Click="BtnTambah_Click"/>
                <ui:Button Content="Buka alamat" Margin="0,0,8,0" Click="BtnBuka_Click"/>
                <ui:Button Content="Buka folder" Margin="0,0,8,0" Click="BtnFolder_Click"/>
                <ui:Button Content="Terminal di sini" Margin="0,0,8,0" Click="BtnTerminal_Click"/>
                <ui:Button Content="npm install" Margin="0,0,8,0" Click="BtnInstall_Click"/>
                <ui:Button Content="Hapus dari daftar" Click="BtnHapus_Click"/>
            </WrapPanel>
        </StackPanel>

        <Border Grid.Row="3" Background="{DynamicResource ControlFillColorDefaultBrush}"
                CornerRadius="8" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" x:Name="TxtJudulLog" Text="Keluaran" Margin="0,0,0,6"
                           FontWeight="SemiBold" FontSize="12" Opacity="0.8"/>
                <TextBox Grid.Row="1" x:Name="TxtLog" IsReadOnly="True" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         TextWrapping="NoWrap" FontFamily="Consolas" FontSize="11"
                         BorderThickness="0" Background="Transparent"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
